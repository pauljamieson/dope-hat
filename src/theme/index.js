import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#040404" },
    secondary: { main: "#fff" },
    info: { main: "#fff" },
    text: {
      primary: "#00b7eb",
      secondary: "#FF00ff",
    },
  },
});

export default theme;
