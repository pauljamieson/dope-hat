import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    //primary: { main: "#040404" },
    primary: { main: "#040404" },
    secondary: { main: "#000" },
    info: { main: "#4f4f4f" },
    text: {
      primary: "#00b7eb",
      secondary: "#FF00ff",
    },
  },
});

export default theme;
